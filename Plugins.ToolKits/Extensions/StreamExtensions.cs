namespace Plugins.ToolKits.Extensions
{
    public static class StreamExtensions
    {
        //public static Stream WriteString(this Stream stream,string stringContent,Encoding encoding)
        //{
        //    if (stream is null)
        //    {
        //        throw  new ArgumentNullException(nameof(stream));
        //    }
        //    if (stringContent is null)
        //    {
        //        throw new ArgumentNullException(nameof(stringContent));
        //    }
        //    if (encoding is null)
        //    {
        //        throw new ArgumentNullException(nameof(encoding));
        //    }

        //    if (stream.CanSeek && stream.CanWrite)
        //    {
        //        var buffer = encoding.GetBytes(stringContent);

        //        stream.Write(buffer, 0, buffer.Length);
        //    }

        //    return stream;
        //}
    }
}